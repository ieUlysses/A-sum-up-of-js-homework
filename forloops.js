for (let counter = 0; counter <= 10; counter +=2) {  //counter is defined inside the for loop to keep the loop working. counter=0 defines start, counter <=10 defines end((when counter si less than or equal to 10)) counter +=2 defines the steps, so plus 2 makes the code go up in 2s ((2,4,6,8,10))
    console.log(counter)
}

/* counter = 0;
while (counter <= 10) {
    console.log(counter);

    counter +=2
} */